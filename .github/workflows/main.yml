on: 
  push

name: 🚀 Deploy website Producao
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ftp.valesis.com.br
        username: valesis@valesis.com.br
        password: Vs949207@#
        